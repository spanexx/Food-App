{
    "name": "foodmine-course",
    "version": "1.0.0",
    "engines": {
      "node": ">=18 <21"
    },
    "scripts": {
      "start": "cd backend/built && nodemon server.js",
      "prebuild": "cd backend && npm run build",
      "build": "cd frontend && npm run build"
    }
  }